name: CI Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Java
      uses: actions/setup-java@v3
      with:
        distribution: 'temurin'  # Use 'temurin' or 'zulu' for JDK distribution
        java-version: '11'  # Use the appropriate Java version

    - name: Build with Maven
      run: mvn clean install -f "Assignment 2/pom.xml"  # Use the correct path to pom.xml

    - name: Run Tests
      run: mvn test -f "Assignment 2/pom.xml"  # Use the correct path to pom.xml
