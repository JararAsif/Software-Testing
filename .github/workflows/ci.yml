name: CI Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Java
      uses: actions/setup-java@v3
      with:
        distribution: 'temurin'  # Use 'temurin' or 'zulu' for JDK distribution
        java-version: '11'  # Use the appropriate Java version

    - name: Build with Maven
      run: mvn clean install -f "Software-Testing/Assignment 2/pom.xml"  # Run Maven build command

    - name: Run Tests
      run: mvn test -f "Software-Testing/Assignment 2/pom.xml"  # Run your tests using Maven
